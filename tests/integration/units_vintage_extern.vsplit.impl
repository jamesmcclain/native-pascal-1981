(*$INCLUDE:'units_vintage_extern.vsplit.inc'*)
IMPLEMENTATION OF vsplit;

{ EXTERN declarations come first, exactly as the manual requires. }
FUNCTION Helper(x: INTEGER32): INTEGER32; EXTERN;

FUNCTION Local(x: INTEGER32): INTEGER32;
BEGIN
  Local := Helper(x) + 1;
END;

BEGIN
END.
