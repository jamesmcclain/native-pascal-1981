(*$INCLUDE:'units_graph.alpha.inc'*)
(*$INCLUDE:'units_graph.beta.inc'*)
(*$INCLUDE:'units_graph.gamma.inc'*)
PROGRAM UnitsGraph(output);
USES beta, gamma;
BEGIN
  WRITELN(BetaValue);
  WRITELN(GammaValue);
END.
