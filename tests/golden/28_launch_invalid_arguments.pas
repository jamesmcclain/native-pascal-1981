{ DIALECT: extended }
PROGRAM LAUNCHINVALIDARGUMENTS;
TYPE PINT = ^INTEGER32;
VAR p: PINT;
PROCEDURE kernel(data: PINT; item: INTEGER32);
BEGIN END;
BEGIN
  LAUNCH(kernel, 1, 1, p);
  LAUNCH(kernel, 1, 1, p, FALSE)
END.
